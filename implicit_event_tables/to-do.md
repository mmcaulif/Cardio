# To do list
* [ ] set up proper experimenting pipeline, benchmark TD3 implementation etc.
* [ ] add central buffer that all transitions are added to and create a sampling ratio between central buffer and event tables
* [ ] concatenate actions, rewards and done boolean to k-means inputs
* [ ] implement encoder architecture (VAE, simese-network) and training